ROR (Rotate right):
ROR, <axis>, <velocity>

ROL (Rotate left):
ROL, <axis>, <velocity>

MST (Motor stop):
MST, <axis>

MVP (Move to Position):
MVP, <ABS|REL>, <axis>, <position|offset>

SAP (Set axis parameter):
SAP, <parameter number>, <axis>, <value>

WAIT (Wait for an event):
WAIT, <POS>, <axis>, <ticks>
ticks --> ticks to wait for timeout, ticks = 0 deactivate
