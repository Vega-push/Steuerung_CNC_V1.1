class Antriebsstrang:
    def __init__(self, steigung, getriebe, y_max, x_max, z_max):
        self.steigung = steigung
        self.getriebe = int(getriebe)
        self.y_max = y_max
        self.x_max = x_max
        self.z_max = z_max
